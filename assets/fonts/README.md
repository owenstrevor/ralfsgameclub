# Fonts

Drop your brand font files here. The site expects:

- `brand.woff2`  ← primary weight (regular/400)
- `brand-bold.woff2`  ← bold weight (700)

The CSS in `index.html` already has @font-face declarations pointing to these filenames.
Swap out the `font-family` fallback stack once files are in place.
